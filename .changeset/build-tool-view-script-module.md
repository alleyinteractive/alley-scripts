---
"@alleyinteractive/build-tool": minor
---

Support the block metadata `viewScriptModule`/`viewModule` field by handling the dual-compiler (array) webpack configuration that `wp-scripts` produces under `--experimental-modules`. Builds without the flag are unchanged.
