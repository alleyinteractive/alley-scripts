name: "Feature Set Two: Feature A"

inputs:
  - name: featureName
    description: "Feature Name"
    type: string

files:
  - source: feature.stub
    destination: src/${{ inputs.featureName | wpClassFilename }}
