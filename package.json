{
  "name": "block-editor-tools",
  "version": "0.1.0",
  "scripts": {
    "publish:check": "lerna updated",
    "publish:patch": "lerna publish --dist-tag patch",
    "publish:prod": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
